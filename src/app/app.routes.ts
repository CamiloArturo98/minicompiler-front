import { Routes } from '@angular/router';
import { CompilerPageComponent } from './pages/compiler-page/compiler-page/compiler-page';

export const routes: Routes = [
  { path: '', component: CompilerPageComponent },
];
